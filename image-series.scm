;;;--wrapper if sam uses marcos

;;;the code is in rainforest!.scm
(load "/home/cowdenda/scheme/final_project/rainforest.scm")

(define image-series
 	(lambda (n width height)
  		(rainforest! n width height)))

;;;---look at rainforest!.scm for documentation
